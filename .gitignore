# extraction source material — never committed
extraction-inbox/

# secrets
.env
.env.*
!.env.example

# local patchback dev settings (written by `patchback init`)
patchback.config.ts

# dependencies & build output
node_modules/
dist/
coverage/
*.tsbuildinfo

# tooling caches
.turbo/
.a5c/
.eslintcache

# OS
.DS_Store
